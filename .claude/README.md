# BABCS Claude Configuration

Claude Code project instructions are maintained in `../CLAUDE.md`, with shared
engineering rules in `../AGENTS.md`.

Keep this directory limited to BABCS-specific settings, commands, hooks, or
skills. Do not store project history, secrets, generated files, dependencies,
or configuration copied unchanged from another repository.

